# lixisphere-site

Distribution de LixiSphere : `version.json` (manifeste de mise a jour lu par l'application via GitHub Pages) + installeurs en assets de releases.

Publie par `installer/publish-update.ps1` du depot principal (prive).